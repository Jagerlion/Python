def steps_to_miles(steps):
    """
    This is a docstring:
    Change this text to explain what your code does, you can go to a new line without issues
    like this
    """

    # 2000 steps is 1 mile
    miles = steps // 2000
    return miles

if name == 'main':
    # Type your code here.
    try:
        # get the steps
        # if steps < 0
        # raise error
    except:
        # exception error message
    else:
        # call function steps_to_miles(steps) and set as variable miles
        # return miles
